#include <string>
#include <vector>

using namespace std;

int get
vector<int> solution(string s) {
    vector<int> answer;

    int length = 0;
    for (int i = 0; i < s.size(); i++) {
        if (s[i] == '1') {
            length++;
        }
    }

    while (1) {

    }
    return answer;
}